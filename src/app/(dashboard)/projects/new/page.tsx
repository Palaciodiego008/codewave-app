import { Box, Typography } from "@mui/material"
import { FormProjects } from "../components/form/Form";

export const NewProjects = () => {
  return (
    <Box>
      <FormProjects title="New Project" />
    </Box>
  )
}

export default NewProjects;
